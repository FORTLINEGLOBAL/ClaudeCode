# Copy this file to `.env` and fill in the real password.
# `.env` is gitignored — never commit the real password.
#
#   cp .env.example .env
#   # edit .env, then:
#   set -a; source .env; set +a
#   python3 send_email.py --to someone@example.com --subject Hi --body Hi

HATUL_SMTP_PASSWORD=your-moneyplan-mailbox-password-here
